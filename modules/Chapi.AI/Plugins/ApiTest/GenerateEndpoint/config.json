{
  "schema": 1,
  "description": "Generate Chapi IR from AuthProfile and Endpoint JSON. Returns structured JSON for a single endpoint test suite.",
  "execution_settings": {
    "gpt-4o": {
      "model_id": "gpt-4o",
      "service_id": "gpt-4o",
      "temperature": 0.1,
      "text_format": "json",
      "store": false
    }
  },
  "input_variables": [
    {
      "name": "auth_profile_json",
      "description": "AuthProfile JSON from Chapi database",
      "required": true
    },
    {
      "name": "endpoint_json",
      "description": "Endpoint JSON from Chapi EndpointCatalog",
      "required": true
    },
    {
      "name": "fallback_base_url",
      "description": "Fallback base URL if not inferrable from auth profile",
      "required": false
    },
    {
      "name": "default_scope",
      "description": "Default OAuth scope fallback",
      "required": false
    }
  ],
  "messages": [
    {
      "role": "system",
      "content": "You are Chapi-IR-GPT. Generate a single Chapi IR (suite.json) for ONE endpoint using AuthProfile and Endpoint JSON. Return ONLY valid JSON that matches the Chapi IR v0.1 format. Include OAuth2 token acquisition if TokenUrl exists, proper auth injection, and comprehensive tests (happy path + negatives)."
    },
    {
      "role": "user",
      "content": "AuthProfile JSON:\n{{$auth_profile_json}}\n\nEndpoint JSON:\n{{$endpoint_json}}\n\nHints:\nBASE_URL fallback: {{$fallback_base_url}}\nSCOPE fallback: {{$default_scope}}"
    }
  ],
  "defaults": {
    "fallback_base_url": "http://localhost:5066",
    "default_scope": "openid email profile roles"
  }
}
